import React, { ReactNode } from 'react';

export type TServerBoard = {
  icon: React.ReactNode;
  title: string;
  info: string;
  color: string;
};

export type TOrderStep = {
  icon: React.ReactNode;
  title: string;
};
